{
  "name": "photosort",
  "version": "1.0.0",
  "description": "This utility takes all image files from a folder and reorganizes them into date based folder tree structure.",
  "main": "index.js",
  "dependencies": {
    "exif": "^0.6.0",
    "typescript": "^4.1.3"
  },
  "devDependencies": {
    "@types/exif": "^0.6.1",
    "@types/node": "^14.14.22",
    "ts-node-dev": "^1.1.1"
  },
  "scripts": {
    "sort": "npm run build && npm run index",
    "index": "node dist/index.js",
    "build": "tsc",
    "dev": "ts-node-dev --respawn index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ZigaBobnar",
  "license": "MIT"
}
